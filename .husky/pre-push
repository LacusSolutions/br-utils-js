export CURR_DIR="$PWD"

cd "$CURR_DIR/packages/cnpj-fmt"
bun x tsc --noEmit
bun run test

cd "$CURR_DIR/packages/cnpj-gen"
bun x tsc --noEmit
bun run test

cd "$CURR_DIR/packages/cnpj-val"
bun x tsc --noEmit
bun run test

cd "$CURR_DIR/packages/cnpj-utils"
bun x tsc --noEmit
